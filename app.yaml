runtime: nodejs22
instance_class: F1
entrypoint: npm start

automatic_scaling:
  min_instances: 0
  max_instances: 1
  min_idle_instances: 0

env_variables:
  NODE_ENV: production

handlers:
  - url: /.*
    script: auto
    secure: always
