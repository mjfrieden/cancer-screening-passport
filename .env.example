# Runtime environment
NODE_ENV="development"

# APP_URL: Public URL used for OAuth callbacks, app links, and share URLs.
APP_URL="http://localhost:3000"

# Firebase client config is currently stored in firebase-applet-config.json.
# For a public launch, create a clean Firebase project and keep production,
# staging, and local configs separated by deployment environment.
